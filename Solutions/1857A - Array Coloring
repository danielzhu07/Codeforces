#include <bits/stdc++.h>
using namespace std;

int main(){
	int T;
	cin>>T;
	while(T--){
		int N;
		cin>>N;
		int odds = 0;
		for(int i = 0; i < N; i++){
			int x;
			cin>>x;
			if(x % 2 == 1)
				odds++;
		}
		if(odds % 2 == 0)
			cout<<"YES"<<endl;
		else
			cout<<"NO"<<endl;
	}
	return 0;
}
